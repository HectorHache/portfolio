# Dash landing on Cloudflare Workers (static assets).
# Worker: hector-dash. Deterministic single file.
name = "hector-dash"
compatibility_date = "2026-09-08"
workers_dev = true

assets = { directory = "." }

routes = [
  { pattern = "dash.hector.app", custom_domain = true },
]
